Stories:

X) There is an app.

2) Display the last picture taken.

3) Send a text (hardcoded content and number).

4) Send the picture by text (hardcoded number)

5) Set the number.
   - menu item for settings
   - create a preferences activity to fetch it.

7) On opening, if no number is set, ask for one.

8) Option to delete the picture after sending.

9) Nice happy icon

10) Select a contact to set the number.


Silly stuff that would be fun:
- default the phone number to the one most frequently texted (or texted pictures)
